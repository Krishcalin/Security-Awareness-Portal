# Server
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
psycopg[binary]>=3.1
psycopg-pool>=3.2
reportlab>=4.0       # renders the certificate PDF
pillow>=10.0         # derives the certificate render source

# Microsoft's own library for the Entra sign-in flow. Validating an ID token
# means checking a signature against a rotating JWKS, the issuer, the
# audience, the expiry and the nonce; every one of those is somewhere a
# hand-rolled version quietly accepts a token it should not.
msal>=1.31
jinja2>=3.1          # renders the sign-in page, with autoescaping

# Recording the narration with Azure Neural TTS. Optional: the `sapi`
# backend needs nothing, and the portal falls back to the browser's own
# synthesiser for any slide with no recording.
#   pip install azure-cognitiveservices-speech

# Tests
pytest>=7.0
httpx>=0.27          # required by fastapi.testclient
aiosmtpd>=1.4        # a real SMTP server, so the send path is tested
pypdfium2>=4.0       # reads back the rendered certificate
